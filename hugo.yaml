baseURL: "https://zags.dev"
languageCode: en-us
title: "zags.dev"
theme: PaperMod

enableEmoji: true

outputs:
  home:
    - HTML
    - RSS
    - JSON # necessary for search

menu:
  main:
    - name: Archive
      url: archives
      weight: 10
    - name: Search
      url: search
      weight: 20

params:
  showToc: true
  ShowCodeCopyButtons: true
  ShowPostNavLinks: true # add navigation buttons at the end of a single post
  ShowBreadcrumbs: true # adds breadcrumb navigation above posts title

  profileMode:
    enabled: true
    buttons:
      - name: Blog
        url: posts

  socialIcons:
    - name: GitHub
      title: "Follow me on GitHub"
      url: "https://github.com/syaghoubi00"

  assets:
    disableHLJS: true # disable Highlight.js to use Hugo native Chroma syntax hl

  cover:
    hidden: true # hide everywhere but not in structured data
    hiddenInList: true # hide on list pages and home
    hiddenInSingle: false # show on single page

markup:
  highlight:
    lineNos: true
    style: gruvbox
